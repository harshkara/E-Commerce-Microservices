#version: '3.8'
#
#services:
#  auth-service:
#    image: auth-service:v1
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_PROFILES_ACTIVE: ${PROFILE}
#    secrets:
#      - auth_db_url
#      - auth_db_user
#      - auth_db_pass
#    command: >
#      sh -c "
#      if [ \"$SPRING_PROFILES_ACTIVE\" = \"prod\" ]; then
#        export DB_URL=$(cat /run/secrets/auth_db_url) &&
#        export DB_USERNAME=$(cat /run/secrets/auth_db_user) &&
#        export DB_PASSWORD=$(cat /run/secrets/auth_db_pass);
#      fi &&
#      java -jar auth-service.jar
#      "
#secrets:
#  auth_db_url:
#    external: true
#  auth_db_user:
#    external: true
#  auth_db_pass:
#    external: true