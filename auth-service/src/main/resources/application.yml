spring:
  application:
    name: auth-service-api
  profiles:
    active: ${SPRING_PROFILE:dev}

logging:
  level:
    root: INFO
    com.practice.authService : INFO
    org.springframework.web : INFO
    org.hibernate.sql : WARN
    org.hibernate.orm.jdbc.bind : WARN