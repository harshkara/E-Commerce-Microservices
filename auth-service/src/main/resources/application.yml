spring:
  application:
    name: task-manager-api
  profiles:
    active: dev

logging:
  level:
    root: INFO
    com.practice.taskmanager : INFO
    org.springframework.web : INFO
    org.hibernate.sql : WARN
    org.hibernate.orm.jdbc.bind : WARN